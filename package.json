{
  "name": "academic-reviewer",
  "version": "1.0.0",
  "private": true,
  "description": "Turn lecture slides and handouts into a graded reviewer you can take offline.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
